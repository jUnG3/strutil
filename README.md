# strutil
String util for C. The lib provides a `trim` and a `strexplode` function.

# Test
Run the configured unit tests.
```bash
make test
```

# Build
```bash
make all
```

# Install
```bash
sudo make install
```
